# snyk-org-repo-mapper

Staged Python CLI for onboarding Bitbucket Server and GitHub repositories into Snyk: Stage 1 discovery (Bitbucket, spreadsheet, or GitHub), org list generation, optional Universal Broker setup, import file enrichment, and post-import cleanup.

See repository [README.md](../README.md) for usage. OpenSpec changes live under `openspec/changes/`.
